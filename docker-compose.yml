services:
  backend:
    build: .
    container_name: backend

    restart: always

    ports:
      - "4000:4000"

    environment:
      NODE_OPTIONS: --max-old-space-size=1536

    env_file:
      - .env